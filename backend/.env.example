# Copy to .env for local development. Never commit the copied file.
APP_ENV=development
AUTH_SECRET_KEY=replace-with-a-long-random-value
MONGODB_URI=mongodb://localhost:27017
DB_NAME=agentic_testing

# Target access policy. Keep empty only for a deliberately open local setup.
TARGET_HOST_ALLOWLIST=localhost,127.0.0.1
ALLOW_PRIVATE_TARGETS=true
HTTP_VERIFY_TLS=true

LLM_PROVIDER=lmstudio
LMSTUDIO_URL=http://localhost:1234
LLM_MODEL=meta-llama-3-8b-instruct
GROQ_API_KEY=

ZAP_IMAGE=ghcr.io/zaproxy/zaproxy:stable
ZAP_TIMEOUT_S=300
PLAYWRIGHT_RUN_TIMEOUT_S=180
ARTIFACT_RETENTION_DAYS=30
ARTIFACT_MAX_BYTES=10485760
